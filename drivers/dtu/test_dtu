#include "DTU.h"

extern volatile int g_radio_power_flag;

int main1()
{
    int ret = -1;

    g_radio_power_flag = 1;

    pthread_t tid;
    
    pthread_create(&tid, NULL, (void *)DTU_TaskThread, NULL);
    while(1);

    return 0;
}

